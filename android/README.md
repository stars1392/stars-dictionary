# Star Tools Android

This directory is generated by the GitHub Actions workflow. The workflow installs Capacitor, copies `tools.html` into the web app, adds Android, and builds a debug APK.

The APK is published as a GitHub Actions artifact named `star-tools-debug-apk`.
